<UserControl x:Class="SeriAllPort.Views.ProfileEditor.ProfileEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SeriAllPort.Views.ProfileEditor"
             xmlns:con="clr-namespace:CommonWpf.Converters;assembly=CommonWpf"
             Height="auto"
             Width="auto"
             
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500"
             d:Background="White"
             >
    <UserControl.Resources>
        <con:BoolInverseConverter x:Key="boolInverse"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*" MinWidth="150"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="70*" MinWidth="200"/>
        </Grid.ColumnDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Profiles"/>
            <StackPanel Grid.Row="1" HorizontalAlignment="Right" Orientation="Horizontal">
                <Button Command="{Binding ProfileUpCommand}" Width="auto">
                    <Image Source="/Resources/Icon/Up.png"/>
                </Button>
                <Button Command="{Binding ProfileDownCommand}" Width="auto">
                    <Image Source="/Resources/Icon/Down.png"/>
                </Button>
                <Button Content="New" Command="{Binding ProfileNewCommand}" Width="auto"/>
                <Button Content="Delete" Command="{Binding ProfileDeleteCommand}" Width="auto"/>
            </StackPanel>
            <ListBox Grid.Row="2"
                     SelectedItem="{Binding SelectedProfile}"
                     SelectedIndex="{Binding SelectedProfileIndex, Mode=OneWayToSource}"
                     ItemsSource="{Binding Profiles}" DisplayMemberPath="Name"
                     />
        </Grid>

        <GridSplitter Grid.Column="1" Width="3" HorizontalAlignment="Stretch"/>

        <Grid Grid.Column="2" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Properties"/>

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Name"/>
                <TextBox Grid.Column="1" 
                     Text="{Binding SelectedProfile.Name}" 
                     IsEnabled="{Binding SelectedProfile.CanNotEditName, Converter={StaticResource boolInverse}}"
                     Width="auto" HorizontalAlignment="Stretch"/>

                <TextBlock Grid.Row="1" Text="Description"/>
                <TextBox Grid.Row="2" Grid.ColumnSpan="2" 
                         Width="auto" HorizontalAlignment="Stretch"
                         Height="auto" VerticalAlignment="Stretch"
                         VerticalContentAlignment="Top"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Visible"
                         
                         Text="{Binding SelectedProfile.Description}"/>

            </Grid>
        </Grid>

    </Grid>
</UserControl>

